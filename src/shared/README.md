# Shared Layer

このディレクトリには、全層で共有されるコンポーネントを配置します。

## 構造

### types/
共通の型定義
- `common.ts` - アプリケーション全体で使用する型
- `api.ts` - API関連の型定義

### utils/
汎用ユーティリティ関数
- `validation.ts` - バリデーション関数
- `formatting.ts` - フォーマット関数
- `constants.ts` - 定数定義

## 原則

- 全ての層から利用可能
- ビジネスロジックを含まない
- 汎用的で再利用性の高い機能